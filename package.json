{
  "dependencies": {
    "@mlc-ai/web-llm": "^0.2.78"
  },
  "devDependencies": {
    "@parcel/config-webextension": "^2.13.3",
    "@parcel/optimizer-terser": "^2.14.2",
    "@types/chrome": "^0.0.309",
    "buffer": "^5.5.0||^6.0.0",
    "parcel": "^2.14.2",
    "process": "^0.11.10"
  },
  "scripts": {
    "start": "parcel watch src/manifest.json",
    "build:dev": "parcel build src/manifest.json --no-optimize --no-scope-hoist",
    "build:prod": "parcel build src/manifest.json --detailed-report --log-level verbose"
  }
}
